<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view"
    android:theme="@style/Theme.Design.NoActionBar">

    <group android:checkableBehavior="single">
        <item android:id="@+id/item_plan"
            android:icon="@drawable/ic_fintness"
            android:title="Training plans">
        </item>
        <item android:id="@+id/item_discover"
            android:icon="@drawable/ic_news"
            android:title="Discover">
        </item>
        <item android:id="@+id/item_report"
            android:icon="@drawable/ic_report"
            android:title="Report">
        </item>
        <item android:id="@+id/item_bmi"
            android:icon="@drawable/ic_bmi"
            android:title="BMI">
        </item>
    </group>
</menu>